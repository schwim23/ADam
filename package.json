{
  "name": "adam",
  "version": "0.1.0",
  "private": true,
  "description": "AI advertising operations agent built on AdCP",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
